Answers

1. shutil.copy() ---> will copy the at the path source to the folder at the path destination.
   shutil.copytree() ---> is used to copy an entire folder along with all its contents.

2. shutil.move() is used to rename files.

3. shutil module will delete the files/folders permanently whereas send2trash will move the deleted files/folders to the recycle bin.

4. zipfile.ZipFile()