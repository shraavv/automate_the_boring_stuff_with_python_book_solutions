Answers

1. dict() or {}

2. {'foo': 42}

3. Dictionary is an unordered sequence, cannot contain duplicate keys whereas list is an ordered sequence, can contain duplicate values.

4. It returns an error (KeyError)

5. Both of them have the same function, they will check if a key named 'cat' exists or not.

6. 'cat' in spam checks whether a key named 'cat' exists or not whereas 'cat' in spam.values() checks whether a value equal to 'cat exists or not.

7. spam.setdefault('color','black')

8. pprint() and pformat() is present in pprint module 